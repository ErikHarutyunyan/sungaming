import React from 'react';
import PropTypes from 'prop-types';
import styles from './GamesFeatured.module.scss';

const GamesFeatured = () => (
  <div className={styles.GamesFeatured}>
    GamesFeatured Component
  </div>
);

GamesFeatured.propTypes = {};

GamesFeatured.defaultProps = {};

export default GamesFeatured;
